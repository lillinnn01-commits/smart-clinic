spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/smart_clinic?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false}
spring.datasource.username=${DB_USER:clinic}
spring.datasource.password=${DB_PASSWORD:clinic123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
server.error.include-message=always
jwt.secret=${JWT_SECRET:SmartClinicSecretKeyForEducationalProject2026MustBeLong}
